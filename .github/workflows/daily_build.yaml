name: Daily build

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          push: true
          tags: grpc-gateway-builder:latest
